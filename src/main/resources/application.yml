
---

spring:
  profiles:
    active: local
    group:
        local: local
        prod : prod
        staging : staging
        debug: debug

management:
  endpoints:
    web:
      exposure:
        include: "*"