
---

spring:
  profiles:
    active: local
    group:
        local: local
        dev : dev
        staging-test : staging-test
        debug: debug

management:
  endpoints:
    web:
      exposure:
        include: "*"